import { LineChart } from 'lucide-react'
import { PagePlaceholder } from '@/components/PagePlaceholder'

export function MonitoringPage() {
  return <PagePlaceholder icon={LineChart} pageKey="monitoring" />
}
