import { ClipboardList } from 'lucide-react'
import { PagePlaceholder } from '@/components/PagePlaceholder'

export function OnboardingPage() {
  return <PagePlaceholder icon={ClipboardList} pageKey="onboarding" />
}
