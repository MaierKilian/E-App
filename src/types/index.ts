/**
 * Zentrale Domänen-Typen der E-App.
 *
 * Hier werden ab Phase 1 die Datenmodelle definiert (z. B. Haushalt, Raum,
 * Messung, Zählerstand). Sie liegen bewusst getrennt von der UI, damit später
 * der Wechsel von lokaler Speicherung zu Konto + Cloud nur die Datenschicht
 * betrifft und nicht die Oberfläche.
 */

export {}
